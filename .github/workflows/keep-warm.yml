name: Keep API Warm

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s --max-time 10 https://fake-news-headline-detector-api.onrender.com/ > /dev/null
